# AI Novel Writer

本文档定义 AI Novel Writer 中稳定且面向用户的产品术语，避免将发布、更新和通知混为同一概念。

## 界面外观

**图片皮肤**：独立于颜色主题的全局界面视觉风格。用户可在经典、二次元和自定义图片皮肤之间切换；不按小说项目分别保存。
_避免_：颜色主题、项目皮肤、远程背景

**自定义皮肤**：用户导入的一张 PNG 或 JPEG 图片皮肤。它不是远程图片链接、SVG、GIF、动态背景或多图轮换。
_避免_：外部图片 URL、动态壁纸、皮肤图库

## 更新分发

**更新源**：
应用直接读取 AI-Novel-Writer 的公开 GitHub Release；不依赖自建更新服务器。
_避免_：更新后台、推送服务器

**官方安装包**：
同一正式 GitHub Release 的资产列表包含 Windows NSIS 安装程序和未签名的 macOS ARM64 DMG；两种官方安装包均只由 GitHub Actions 构建和上传，不再发布或维护便携 ZIP。Windows 安装程序仍是应用内更新的唯一目标；macOS 不提供应用内自动更新，用户通过该 Release 列表手动下载新版 DMG。
_避免_：按平台拆分正式发布、从本机构建或上传官方安装包、把 macOS DMG 误作 Windows 更新资产、并行维护便携版覆盖运行中文件

**便携版迁移**：
已使用旧便携 ZIP 的用户需手动下载并安装首个包含更新功能的正式安装包；此后，安装版才会下载官方安装程序并完成后续应用内更新。不在原便携目录内执行覆盖更新。
_避免_：直接替换运行中的应用文件、承诺旧便携版可自行获得首个更新器版本

**更新功能首发**：
首个包含应用内更新功能的正式版需要由旧版本用户手动安装一次；安装该版本后，后续正式版才可通过应用内更新获得。
_避免_：承诺旧版本可自行获得首个更新器版本

**更新检查**：
应用运行时对更新源执行的版本比较，可由用户点击首页按钮主动触发。
_避免_：后台推送、系统推送

**自动检查**：
应用启动后执行的静默更新检查；同一设备每天最多一次，用户手动检查不受此频率限制。
_避免_：每次启动重复联网、仅依赖手动检查

**检查失败处理**：
自动检查或后台下载失败时不向用户展示打扰性错误，在符合“每天最多一次”限制的后续自动检查中再重试；用户主动检查时才展示失败原因和重试操作。
_避免_：网络波动反复弹窗、静默吞掉手动检查失败

**可用更新**：
仅适用于 Windows 应用：更新源中版本号高于当前已安装版本、且同时包含 Windows 安装程序与 Windows 更新元数据的正式发布版本。macOS DMG 不参与该判定。
_避免_：任意 GitHub 提交、草稿发布、把 macOS DMG 当作 Windows 更新

**macOS 手动更新**：
macOS ARM64 用户从同一正式 GitHub Release 的资产列表手动下载并安装新版 DMG；应用不检查、下载或安装 macOS 更新。
_避免_：承诺 macOS 应用内更新、为 macOS 维护 Windows 更新元数据

**Gatekeeper 提示**：
macOS ARM64 DMG 未进行代码签名或公证，macOS 可能显示 Gatekeeper 风险提示；该提示不表示该包具有签名信任，也不构成绕过系统安全提示的承诺。
_避免_：宣称未签名包无系统提示、把 GitHub Release 来源等同于代码签名

**更新提示**：
应用在发现可用更新后展示给正在使用软件的用户的界面提示；不表示应用关闭时的系统通知。
_避免_：离线推送、强制更新

**版本更新通知**：
第一版仅在发现可用更新时向正在使用应用的用户展示更新提示；不发送与版本无关的活动、公告、营销或调研消息。
_避免_：运营推送、通用消息中心

**提醒延后**：
用户将某个可用更新的提示延后 7 天或 30 天。延后期间应用仍可执行每日一次的静默检查，但不再次展示该更新的提示；关闭提示卡默认延后 7 天。
_避免_：每天启动重复提醒、永久忽略更新

**应用内更新**：
应用下载可用更新的安装包，并在下载完成后让用户选择立即重启安装或稍后处理。
_避免_：跳转浏览器手动下载、静默强制重启

**更新数据保护**：
更新程序只替换应用本身，不删除小说、角色卡或用户设置等本地数据。用户选择立即重启更新时，应用先处理未保存内容。
_避免_：更新时清除用户数据、未保存内容丢失

## 模型请求参数

**模型配置温度**：
用户在生成模型配置中保存的采样温度，是连接测试、普通生成、流式生成、工作流和 AI 助手共用的唯一用户可调温度来源。工作流步骤不能为特定任务静默覆盖它。
_避免_：连接测试固定 0.7、工作流或 AI 助手各自设置温度、请求级温度入口

**服务商固定温度模型**：
当服务商为某一官方端点和模型声明固定温度规则时，请求参数策略不发送 temperature，由服务商按其规则决定；该例外不适用于名称相同的第三方代理端点。
_避免_：在每个工作流中硬编码服务商特例、把代理端点误判为官方端点

## 项目与创作数据

**角色名单**：
小说中角色身份、角色定位、角色资料与角色关系的结构化事实集合；角色管理、蓝图和写稿门禁只读取角色名单。
_避免_：从角色图谱文本反向猜测角色、把 Markdown 标题当角色身份

**角色图谱**：
根据角色名单确定性生成、供作者阅读的角色关系与弧光投影；它不是角色身份的事实源，也不用于反向创建角色卡。
_避免_：角色名单、可解析的角色卡来源

**旧角色图谱原文**：
升级前项目中尚未结构化的角色图谱文本；系统保留它作为迁移证据，只有显式修复成功后才建立角色名单。
_避免_：自动重复解析、无验证覆盖角色卡

**项目根目录**：
含有效项目清单的小说项目目录；用户选择的任意文件夹不是项目。
_避免_：项目路径、任意目录

**项目会话**：
一次打开项目后的有效工作上下文；切换或重新打开后，旧项目会话失效。
_避免_：仅以路径代表当前项目

**外部文件授权**：
用户选择文件或目录后，应用针对特定操作与范围授予的临时访问权。
_避免_：外部路径、任意文件权限

**定稿提交**：
将用户确认的正文快照写入项目事实源的操作。
_避免_：定稿文件写入

**实体稿发布**：
将已定稿内容同步为可供外部读取的章节文件；发布可失败并重试，但不改变定稿事实。
_避免_：把数据库提交和文件写入视为同一原子操作

**嵌入空间**：
由嵌入模型、向量维度与比较语义共同定义的可比较向量集合；更换嵌入空间必须重建索引。
_避免_：向量表、固定 2048 维
