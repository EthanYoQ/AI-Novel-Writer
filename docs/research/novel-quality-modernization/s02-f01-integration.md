# S02 / F01 集成记录

本记录描述 S02/F01 提交时的过渡状态；后续启动接线与真实 Electron 验证另见 S03 集成记录。

S02 提交 `d72c13db638f1f2d87db3230e985411566139fca`，F01 提交 `e2af35966adae4754b18ac7553544a618fa3dc34`；两片均基于 S01 `7143d301313b95a2df572a3b17f1d459b48d5947`。G01 台账已在 `8ceba0964ec2475bcc5454d291adc69b833eb2ca` 独立完成。实施仍在 Draft PR #230，尚未合并或发布。

S02 已将 preload、Window 类型、IPC client、定稿直连、当前发布探针和测试夹具统一为 `aiNovelAPI`。缺新接口时明确拒绝操作，项目租约与授权边界未放宽。集中 parser 兼容读取旧 URI，打开旧标签后生成规范 URI 并保留 dirty 正文；新状态不再写旧 URI。旧安装版专用 `probe-legacy-project-open.mjs` 保留旧接口，它不被当前应用作为后备入口调用。

F01 提供经过 mainReady、皮肤快照及 readback ack 的唯一外观 writer，旧 theme/ui-version 键保持原字节。当前主入口尚未在 S03 接通真实 bootstrap，因此主题初始化及偏好 setter 在 pending 状态不生效。这是实施中的过渡提交，不能单独宣传主题功能保持或发布资格；S03 必须先完成全局裁决，再接通 App 并复验主题/字体/缩放。

独立审查冻结了 S02 116 个路径及 F01 8 个文件，提交前逐项 SHA256 验证无漂移。S02 独立桥接 6、资源/dirty/merge 51、中文 VersionHistory 浏览器 1、工作流夹具 37 项通过；F01 独立单元 33、实际 Chromium localStorage 16 项通过。主线程类型与 lint 检查通过。

第一次完整本地 Node 回归为 300 文件、2811 通过、28 失败、9 跳过，退出 1。26 个失败来自四组旧工作流测试依赖未注入接口的静默回退；仅补明确的合成 bridge，独立复跑四文件 37/37，保持未知 invoke 拒绝及原断言。另两个 Windows 运行时检查超时：安装配置文件单独复跑 6/6，60ms 后代进程失败检测单例复跑 1/1（30 个未选用例不计通过），没有调整生产逻辑或放宽超时。原完整运行自然结束，未终止其进程；修正后的完整云端结果需另行核对，不将分组复跑之和冒充一次全量通过。

模型调用仍为 0/80。真实 Electron 启动、用户数据迁移、安装包、Writer 完整动作与模型质量均不在本次通过范围。原始运行日志与逐文件审查收据留在任务缓存，不进入提交。
