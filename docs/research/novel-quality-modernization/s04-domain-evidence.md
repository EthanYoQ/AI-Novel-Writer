# S04 project migration domain evidence

Dependency: `003dc9b36a081febcb3def739e0474ae8c8d46d9` (S03). This slice enables synthetic fixtures only. It does not authorize migration of an author's project. Model calls: 0.

`migrateProjectFormat` requires a process-local synthetic permit bound to a repository fixture root, explicit writer exclusion, lifecycle closure, and injected central SQLite/vector/manifest adapters. Ready results are frozen and registered in a private WeakSet; `isAdmittedProjectMigration` rejects a cloned or fabricated result. The controller refuses ordinary legacy adoption without the future qualified entry. Canonical target discovery is blocked while a known migration journal has not reached switched.

The private journal has prepared, verified, legacy-isolated, target-installed, switched and restored states. Recovery checks physical source/staging/backup/target facts and content fingerprints. The two directory renames are separate operations, not a directory-wide transaction. Verified backups are retained; source reappearance or changes block reuse. After switched, canonical author edits remain authoritative and old backup contents are never imported again. Required linked assets, unknown schema, changed domain values, unknown dual roots, tampered targets, and lost writer exclusion fail closed. Windows file flushes and rename recovery do not qualify hardware power-loss behavior or arbitrary concurrent reparse replacement.

SQLite uses the central `sqlite-project-migration` adapter and the sole desktop registry. The M00 factory only injects central baseline callbacks: no copied DDL, no later migration step, no author-data backfill. A real test creates a WAL-bearing synthetic database, seals a physical copy, closes the seed writer, then exercises the entire coordinator. DB, WAL, SHM and every other source file match the isolated legacy backup byte-for-byte. Canonical user_version is 1; original paragraph bytes and a deliberately stale word_count of 777 survive. The vector adapter opens only a private physical copy. A real Lance documents/chunks corpus survives and supports Chinese text search after the original import file is absent, with all native handles explicitly closed.

Canonical raw assets: prompts, skills, writing-skills.json, partial_arch, partial_arch.json, chapter_creation_log.json and avatars. Lance tables and registry use the vector adapter. `vectors.json.migrated` is a backup-only historical recovery artifact: its exact bytes remain inside the complete isolated legacy root, it is counted in backupOnlyCount, it is not replayed or copied into active canonical storage. Unknown top-level objects are also retained in that backup and counted. Pending vectors.json or migration journal conversion remains unsupported and blocks; no embedding call repairs it automatically. Private journal content hashes and source paths are not portable receipts or public evidence.

Validation: 81 tests passed across project-format-migration (26), project-access, project-storage-preflight and project-controller-context; Earlier TypeScript noEmit passed; latest shared-tree check reports five stale two-argument fixture calls under central adaptation, with no domain diagnostic. `node scripts/project-migration-acceptance.mjs --run-synthetic` passed 3 suites / 47 tests before the final restored-state regression. Nine interruption checkpoints cover journal lag and physical recovery. The fixture integration explicitly overrides the conservative path budget to 4096 to exercise this installed runtime; this does not qualify older binaries or Windows path limits. The default preflight remains conservative and includes the longest vector staging path.

Unexecuted: real author migration, old binary reopening, S14C qualification, hardware power loss, an external writer exclusion protocol, and end-to-end business repository admission. The close callback in the integration fixture represents already-closed explicitly owned handles; it is not evidence of cross-process lock ownership. Parent owns actual database/locator admission, and later qualification must provide the real writer/binary evidence before any author permit exists.
Review counterexamples closed: manifest-less WAL discovery never creates source SHM; switched manifest schema/identity/date and journal root are revalidated; external journal-directory junction is refused; restored requires physical source and no target/backup conflict while preserving later author edits. Real renderer partial_arch.json candidate and chapter_creation_log.json prefill bytes are retained as non-executing canonical assets. Controller create/open mock distinguishes explicit creation from admission and preserves rollback/session assertions.
