# S01：共享契约与迁移序列实施

依赖为 S00 `657ce4aa5f2e13dda4be222e256cd3faaecc9559` 及测试入口集成 `b90b91961bd301e271f8198280c7494f6534186c`。本片已通过 Astra/Medium 独立审查，无未关闭阻断。

五个共享模块定义作者动作与物理 reservation、候选与来源、稳定身份/派生字段 CAS、审稿结果、存储与便携读取权限。现有 author/derived provenance 和定稿身份仍为事实来源。纯契约不直接接入数据库、IPC 或生成流程；后续切片按 [模块所有权](../../adr/0018-module-ownership.md) 接线。

唯一迁移序列为 M00 `0→1`、M01 `1→2`、M02 `2→3`、M03 `3→4`、M04 `4→5`、M05 `5→6`。当前没有安装业务迁移或已识别的旧库指纹，缺步骤和未知库会拒绝执行，不能用空回调升版本。探测、事务迁移、只读验证和未来 session fencing 分离；同版本重开仍须通过领域验证。

[存储合同](s01-storage-contract.json) 对现有资产与便携字段签署处置。通过生产数据库初始化和蓝图延迟建表路径构造合成数据库，实际 SQLite 枚举为 35 表、356 字段，与清单逐项相等；比 S00 初扫补充 5 表、34 字段，并保留来源说明。作者原文与当前可读权威须保留；机器权限、秘密元数据及含这些字节的旧 receipt hash 不进入便携副本，旧执行保持不可重放。

实际检查：

- 主线程类型检查退出 0；四个测试文件共 53/53（领域 14、迁移 22、S00 入口 10、中文计数 7）。
- 独立审查复跑 S01 36/36 和类型检查，最终 12 个受审文件哈希无漂移。
- 规划、正文、审稿三个选定中文生产入口回归 3/3；其他未选择案例不计通过。
- 独立复现并修复：派生状态缺少来源顺序时的旧补丁放行、非法审稿锚点/空结果身份、非法来源/root 身份、便携字段遗漏、同版本重开跳过领域验证。

S00 的云端测试发现冻结文档脚本被直接作为 Vitest suite 加载。已按原 Node 入口接入全部 15 个合同反例，再从普通 suite 发现范围排除冻结规划目录；当前目录仅有这一份检查脚本。新适配经过独立 10/10 复验，冻结字节不变。旧云端运行的 2747 个测试通过、一个 suite 发现失败如实保留，新提交的云端结果另行核对。

本片不等于真实迁移、自动角色更新事务、便携归档/恢复、Writer 或模型资格已完成。业务 M00–M05、当前数据库接线、启动 fencing、真实两臂和全部后续产品门仍由相应 owner 实施。物理模型调用仍为 0/80。本地原始审查/运行收据在任务缓存 `review-s01.json` 等文件中，未提交。
