# S14D — 同 SHA 三平台桌面资格与交付门

执行模型：Sol/high。状态：NOT STARTED；依赖满足后可派发。
Blocked by: S14B, S14C。开工填写依赖实际 SHA。

必读：[通用执行合同](../05-SOL-EXECUTION.md)、[约束性契约](../03-CONTRACTS-AND-GATES.md)（ADR0016、当前.release/release-profile.json；不授予正式发布权限）、[执行矩阵](../04-EXECUTION-MATRIX.md)。

## 目标与非目标

分别报告Windows x64、macOS ARM64、macOS x64资格；最终候选与质量/升级证据必须同SHA。 DSH、真实小说改写与正式发布不在本片。

## 文件所有权

只读运行已在S13/主集成者及S14A冻结前合入的 `.release/`/`.github/` 资格配置；本片只拥有隔离构建/private receipts及报告，不改任何tracked源码/脚本、版本号、tag或Release元数据。

你不是唯一开发者；不改其他片/共享区，不回滚他人改动，不再开子代理。共享接线只交一个明确请求给主集成者。

## 实施步骤

1. 读取最新release profile与三个资格workflow，校对Windows install/launch/error-dialog/uninstall/upgrade/native ABI/packaged-smoke/signing和mac dmg/packaged-smoke/signing的实际要求。
2. 在获准远端提交/工作流后才调云端资格；当前只持有本地Spec不等于授权推送或触发。Mac证据必须来自真实对应架构，不能Windows模拟代替。
3. 按实际profile验证artifact/receipt/SHA/hash，签名策略如允许unsigned需明确披露；中文smoke覆盖当前入口，不默默测试英文替代。
4. 缺B/C任一相同subjectSha终态收据只能blocked/not-qualified；tracked适配/修复回原owner→S14A，产生新subjectSha后旧B/C/D证据全部不自动继承，须同新SHA重跑或记未执行。报告提交SHA独立，不把report commit当被测源码。每阶段重试遵守profile，不无限cloud重跑。

## 验收与测试

同一SHA、三目标各自全部必需receipts；安装后无错误窗、核心路径可用、原生依赖匹配、升级资产不丢；缺签名/平台/权限/receipt据实blocked/not-qualified，不把build成功称release-ready。

先通过 S00 ABI/environment profile；以下为实施时执行，本轮未运行：

```powershell
Get-Content -Raw .release/release-profile.json
Get-Content -Raw .github/workflows/windows-cloud-build-test.yml
Get-Content -Raw .github/workflows/macos-arm64-cloud-build.yml
Get-Content -Raw .github/workflows/macos-x64-cloud-build.yml
```

## 失败、回退与交付

本片不执行promotion workflow，不merge、不打tag、不关闭Issue；尚无云端或远端写授权时停止于待授权/未执行，不绕过release gate用临时命令发布。

必交：最终SHA与代码/中文质量/升级/Vela覆盖/三平台资格独立报告、全部未执行与阻塞；发布需要后续明确决策。按通用合同附 base/dependency/final SHA、实际命令/退出码及 failed/blocked/not-run；代码、确定性、模型、安装资格分别报告。
