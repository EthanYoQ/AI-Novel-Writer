# S09C — 角色 UI 与 Agent 的全入口稳定引用

执行模型：Sol/high。状态：NOT STARTED；依赖满足后可派发。
Blocked by: S09B, S06D。开工填写依赖实际 SHA。

必读：[通用执行合同](../05-SOL-EXECUTION.md)、[约束性契约](../03-CONTRACTS-AND-GATES.md)（C04；ADR0007/0008）、[执行矩阵](../04-EXECUTION-MATRIX.md)。

## 目标与非目标

用户改名/看关系图/批准角色及 Agent读取建议都以 ID保持一致，不让普通用户承担数据库身份复杂度。 DSH、真实小说和正式发布不在本片。

## 文件所有权

`src/stores/{character-store,character-rename-ledger}.ts`、`src/services/character-roster-client.ts`、`src/components/editor/{CharacterEditor,RelationshipGraph}.tsx`、`src/components/panels/sidebar/CharactersView.tsx`、`src/components/characters/CharacterCardImportButton.tsx`、Agent read/propose roster消费者及 tests。共享IPC只请求接线。

你不是唯一开发者；不改其他片/共享区，不回滚他人改动，不再开子代理。共享接线只有一个请求区，由主集成者落地。

## 实施步骤

1. 列表/选中/编辑/关系图以稳定 ID为key，显示名自由改；重名显示足够来源区分，图仍只读派生。
2. 复用现有批量采用/候选UI，提示 unresolved/ambiguous 并让作者选映射或新建，不默认猜；无逐人强制弹窗。
3. 导入按钮/Agent提议绑定项目/session/来源，重复批准幂等；generic filewrite不能绕 roster事实写入。
4. 核销所有name-only正式写消费者，包括恢复/Agent工具；旧用户正文中的名字不自动搜索替换。

## 验收与测试

同名两人选择不串卡、改名关系不丢、名字交换历史仍对人、共享别名待确认、批量采用/取消/重试、项目切换旧UI写拒绝、Agent不能按name猜身份。

先通过 S00 ABI/environment profile；以下为实施时执行，本轮未运行：

```powershell
pnpm run typecheck
pnpm run check:i18n
pnpm exec vitest run src/stores/__tests__/character-project-context.test.ts src/services/agent/tools/__tests__/project-read-tools.test.ts
pnpm exec vitest run --config vitest.browser.config.ts src/components/editor/__tests__/CharacterEditor.relationships.browser.tsx src/components/editor/__tests__/RelationshipGraph.browser.tsx src/components/characters/__tests__/CharacterCardImportButton.browser.tsx
```

## 失败、回退与交付

保留未确认 proposals，UI不得以默认选首项消除歧义；使用现有lucide图标体系，不引入第二套设计/编辑器。

必交：中文角色/关系/批准生产UI证据、全部正式身份消费者退出清单。按通用合同附 base/dependency/final SHA、实际命令/退出码及 failed/blocked/not-run；代码、确定性、模型、安装资格分别报告。

