# S07 — 按任务规划预算、范围与透明诊断

执行模型：Sol/high。状态：NOT STARTED；依赖满足后可派发。
Blocked by: S06D。开工填写依赖实际 SHA。

必读：[通用执行合同](../05-SOL-EXECUTION.md)、[约束性契约](../03-CONTRACTS-AND-GATES.md)（C01/C06；主计划 §4.3）、[执行矩阵](../04-EXECUTION-MATRIX.md)。

## 目标与非目标

解除不合理通用 4K/8K 阶段帽，同时保持模型/上下文/父预算边界；让几千字任务不因内部保留额度假耗尽。 DSH、真实小说和正式发布不在本片。

## 文件所有权

拟新增 `src/services/generation/task-budget-planner.ts`、`electron/llm/generation-parameter-policy.ts` 的能力映射 seam、`src/services/generation/prompt-budget-failure.ts`、`src/components/settings/ModelSettings.tsx` 与现有 AIOutputPanel 诊断呈现。runtime ledger/核心 IPC 禁改；需要调用签名交集成者。

你不是唯一开发者；不改其他片/共享区，不回滚他人改动，不再开子代理。共享接线只有一个请求区，由主集成者落地。

## 实施步骤

1. 实现版本化 policy：阶段目标、中文单位/结构项需求、输入估计上界、模型能力证据、用户限制、父剩余额、max output/批量与每个裁决原因。不是按模型名字猜能力。
2. 预估超容量先拆范围/任务，不把完整世界观裁成正文输入、不粗算字数再设置低于需求的输出帽；单项无法容纳则可解释冲突或分段候选。
3. receipt 展示 requested/reserved/actual/unknown、输入/输出/reasoning，不复印 prompt/密钥；区分容量预检、预算用完、provider length、网络和存储错误。
4. 执行 C06 第一个小型中文对照门：字数目标、规划范围及调用数。冻结数值/版本，未达到 stop/go 不推进最终清理。
5. 本片直接负责 `early-budget` 双目标：消费S00可运行baseline/runner/protocol，取得本片已集成candidate的代码/产物/driver SHA与隔离启动manifest，写本门targets清单；零模型dry-run验证实际目标不同、入口可达、根隔离及语义fixture parity，再运行两臂。单臂smoke、同目标两次、历史旧输出、缺baseline/parity/arm-target receipt均不得PASS；真实调用共用80次帽。

## 验收与测试

模型能力已知/未知、用户128K但模型不支持、4K/8K历史例、输入密集重试、reasoning包含/独立、usage缺失、父剩余不足、200章分批不重写；计数仍用 v3 ±20%，无1120硬门。

先通过 S00 ABI/environment profile；以下为实施时执行，本轮未运行：

```powershell
pnpm run typecheck
pnpm run check:i18n
pnpm exec vitest run src/services/generation/__tests__/task-budget-planner.test.ts src/services/generation/__tests__/prompt-budget-failure.test.ts electron/llm/__tests__/generation-parameter-policy.test.ts src/shared/__tests__/draft-units.test.ts
node scripts/quality-modernization-run.mjs --protocol test/fixtures/novel-quality-modernization/protocol.json --targets .runtime/.cache/novel-quality-modernization/early-S07-targets.json --phase early-budget --dry-run
node scripts/quality-modernization-run.mjs --protocol test/fixtures/novel-quality-modernization/protocol.json --targets .runtime/.cache/novel-quality-modernization/early-S07-targets.json --phase early-budget
```

## 失败、回退与交付

新数值 policy 可停用当前动作并保留收据，但不恢复两套预算。若真实模型门失败只修改此片可证根因，保留所有运行记录；task-budget-planner 测试拟新增。

必交：冻结policy/选择原因、确定性集、early-budget实际命令/双target manifests/零模型dry-run/parity、逐请求arm+target/driver SHA receipts与两臂中文结论。not-run/failed/inconclusive不计通过，early SHA不能代替最终subjectSha。附base/dependency/final SHA与退出码，代码、模型、资格分报。
