# S00 — 最新基线、Vela 全量台账与中文质量预注册

执行模型：Sol/high。状态：NOT STARTED；依赖满足后可派发。
Blocked by: 无。开工填写依赖实际 SHA。

必读：[通用执行合同](../05-SOL-EXECUTION.md)、[约束性契约](../03-CONTRACTS-AND-GATES.md)（主计划 §2、C06、C09）、[执行矩阵](../04-EXECUTION-MATRIX.md)。

## 目标与非目标

形成后续所有切片共同消费的事实基线、全量消费者/资产台账与可复算中文质量 oracle；本片不修生产代码。 不扩展到 DSH、真实小说改写或正式发布。

## 文件所有权

`docs/research/novel-quality-modernization/`（拟新增公开安全说明）、`test/fixtures/novel-quality-modernization/`（拟新增合成 fixture）、`scripts/quality-modernization-run.mjs` 及其专属测试（拟新增实验入口）。运行私有 receipts 放项目 `.runtime/.cache/novel-quality-modernization/`，不提交凭据/作者资料。脚本仅是可重用测试入口，不是生产运行框架。

你不是唯一开发者；不改其他片/共享区，不回滚他人改动，不再开子代理。接线以本合同的单一共享请求交主集成者。

## 实施步骤

1. 核对 actual worktree/远端/发布 SHA 和脏改动，确认 CodeGraph 指向当前源；覆盖 src、electron、scripts、.release、.github、package/lock、活跃文档，排除 DSH/缓存。分别做 Vela 词法与继承行为台账，逐项记录消费者、替代 owner、验收、允许 legacy 位置。
2. 将每个项目/全局资产填 C09 disposition；必需对象未分类是 S04 阻断。查动态 import、barrel、Vite glob 后才标死代码；不只数匹配数。
3. 预注册 3×3 章中文材料、900/2000/3000 目标、事实 oracle、复述标准、双盲评/仲裁和调用分配。保存 baseline 代码/模板/Skill/素材/模型参数指纹；先算 80 请求能否覆盖必要路径，不够报不足。
4. 冻结独立 Node/Electron ABI 测试 profile、浏览器端口和旧安装版本来源；只用合成副本运行基线，记录历史 #221/#226 复现/未复现/缺信息，不把猜测当根因。
5. 同时交付 `protocol.json` 与可执行实验入口（help/dry-run 零模型调用、按协议选择 early/full 场景、维护 80 次总账与安全输出）。复用现有生产入口测试 driver，不能绕过 harness 直接 API 来证明产品质量。S07/S10B/S11 消费该入口；S14B 只读执行最终对照，必要适配都在 S14A 冻结前完成，不等到最后才有实验工具。
6. 按通用合同交付可再次启动的 `baseline-execution-manifest`：冻结旧代码/构建/driver SHA、ABI/启动命令/入口及隔离根，由主线程准备已注册独立工作树或哈希产物，不改当前脏工作树。定义显式双 target runner 输入、同语义源生成旧/新两格式 fixture 的 parity receipt；dry-run 必须验证实际目标身份不同、生产入口可达、根不相交且零模型调用。早期各片提供各自 candidate target，最终 target 由 S14A 冻结，不能拿 S00 旧输出代替最终同时间窗 baseline。
7. S00 尚无改造后 candidate，不制造依赖循环：本片实际完成无模型 baseline 启动/入口探针，并用合成 manifest 测试双目标身份/parity/拒绝逻辑；真实双目标 dry-run 留给有对应候选的 S07/S10B/S11 与最终 S14A。未实际完成双目标验证不得写通过，不能用 baseline 两个副本冒充候选。

## 验收与测试

台账每个生产消费者和磁盘项都有 disposition/owner；已知 `.vela/prompts`、writing-skills、partial_arch、LanceDB 文本、MCP/skin/update 均非遗漏。每个文学 verdict 可由 oracle/rubric 重算；真实测试未跑时明确 not-run。

命令须先通过 S00 的 ABI/environment profile；以下为实施时执行，本轮未运行：

```powershell
git status --short
git worktree list
git rev-parse HEAD
node --version
pnpm --version
pnpm run typecheck
pnpm exec vitest run src/shared/__tests__/draft-units.test.ts
```

## 失败、回退与交付

只增合成材料/证据，不改生产逻辑/真实小说。不可用环境或未知资产会阻塞相关后续片，但可继续其他只读盘点。

必交：inventory.json、asset-disposition.json、quality-protocol.md、protocol.json、实验入口 help/dry-run 与生产 driver 验证、可运行 baseline-execution-manifest、arm fixture parity 合同、environment-profile.md 与安全 evidence index；实际模型结果单列，不承诺本片已修 Bug。另按通用合同交 base/dependency/final SHA、测试退出码/入口覆盖及 failed/blocked/not-run。代码检查、确定性、真实模型和桌面资格不能互相代替。
