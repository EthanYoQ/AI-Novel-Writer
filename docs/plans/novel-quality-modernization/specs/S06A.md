# S06A — 结构化规划入口接入运行与恢复

执行模型：Sol/high。状态：NOT STARTED；依赖满足后可派发。
Blocked by: S05。开工填写依赖实际 SHA。

必读：[通用执行合同](../05-SOL-EXECUTION.md)、[约束性契约](../03-CONTRACTS-AND-GATES.md)（C01/C02；身份批准语义另由 S09A 接管）、[执行矩阵](../04-EXECUTION-MATRIX.md)。

## 目标与非目标

世界观、架构、角色规划、大纲和单字段等入口都经过统一运行边界；本片不改角色事实 schema、不另设阶段硬帽。 DSH、真实小说和正式发布不在本片。

## 文件所有权

`src/services/workflows/commands/{architecture,directory,planning-material,generate-field,analyze-style}.command.ts` 及其专属 helper/tests；角色生成阶段在当前 architecture 实现内按 S00 消费者表覆盖。不改正文/batch/Agent store 或核心 ledger。

你不是唯一开发者；不改其他片/共享区，不回滚他人改动，不再开子代理。共享接线只有一个请求区，由主集成者落地。

## 实施步骤

1. 逐项对接 root action 与冻结输入，将 renderer 自有 attempt/终态/失败保存改为主 owner 收据。
2. 大纲按用户范围/成功覆盖键生成与恢复；只提交完整验证项，JSON 残片保留可见候选。已有世界观候选和压缩上下文修复先复验再复用，不能重做。
3. length/取消/网络失败返回原因与持久候选，不把未完过程当成功；恢复不重写已完成章节范围。
4. 保留当前角色写入批准边界直到 S09A 接力，标记未迁身份消费者，不能提前宣传角色问题已修。

## 验收与测试

10/50/200 章范围以 stub 检查拆解/去重；未选总章数不猜 200；流式截断、坏 JSON、取消、重启、源设定变化、已有世界观恢复案例；每次请求计入一个 root，无完整项重复提交。

先通过 S00 ABI/environment profile；以下为实施时执行，本轮未运行：

```powershell
pnpm run typecheck
pnpm exec vitest run src/services/workflows/commands/__tests__/structured-planning-runtime.test.ts src/services/workflows/commands/__tests__/world-building-recovery.command.test.ts src/services/workflows/__tests__/architecture-workflow-project-context.test.ts
```

## 失败、回退与交付

新 shared owner 不回退为入口私有账本；失败保留独立候选。structured-planning-runtime 测试拟新增，不对 200 章发真实模型请求。

必交：结构化入口接入台账、范围/候选/receipt 对照、针对性测试。按通用合同附 base/dependency/final SHA、实际命令/退出码及 failed/blocked/not-run；代码、确定性、模型、安装资格分别报告。

