# S06B — 正文与批量写作统一恢复

执行模型：Sol/high。状态：NOT STARTED；依赖满足后可派发。
Blocked by: S06A。开工填写依赖实际 SHA。

必读：[通用执行合同](../05-SOL-EXECUTION.md)、[约束性契约](../03-CONTRACTS-AND-GATES.md)（C01/C02/C03；ADR0003/0013）、[执行矩阵](../04-EXECUTION-MATRIX.md)。

## 目标与非目标

正文和两种批量模式统一预算、取消、恢复，成功保存的前章能衔接，失败稿不能污染定稿。 DSH、真实小说和正式发布不在本片。

## 文件所有权

`src/services/workflows/commands/generate-draft.command.ts`、`src/services/workflows/batch-chapter-workflow.ts`、`src/stores/workflow-store.ts`、`src/components/panels/AIOutputPanel.tsx` 及专属测试。S08 仅可并行改角色 repository；共享类型/IPC 不归本片。

你不是唯一开发者；不改其他片/共享区，不回滚他人改动，不再开子代理。共享接线只有一个请求区，由主集成者落地。

## 实施步骤

1. 正文物理调用、重试、续写都归主 owner；UI 显示可见/已保存进度和可查看冲突候选。
2. 批量草稿待审只传同 lineage 合法直接前驱及 hash；自动定稿模式继续现有 snapshot/outbox，不能把待审候选悄悄定稿。
3. 只有一个 cancel/terminal owner，renderer store 由收据派生；磁盘失败停请求，重复进度事件不二次推进章节。
4. 恢复按所选候选/覆盖范围继续，源稿新编辑或依赖变化拒绝正式覆盖，保留新旧可比较成果。

## 验收与测试

900/2000/3000 目标的 stub 输出经同 countDraftUnits 计数；流式中断/重启/取消；两批量模式；旧候选/跨 batch/前章被替换；自动定稿失败 outbox 可恢复且不重复正文。

先通过 S00 ABI/environment profile；以下为实施时执行，本轮未运行：

```powershell
pnpm run typecheck
pnpm exec vitest run src/services/workflows/commands/__tests__/generate-draft.command.test.ts src/services/workflows/__tests__/batch-chapter-workflow.test.ts src/stores/__tests__/workflow-store-generation-budget.test.ts
pnpm exec vitest run --config vitest.browser.config.ts src/components/panels/__tests__/AIOutputPanel.recovery.browser.tsx src/components/dialogs/__tests__/batch-chapter-completion-mode.browser.tsx
```

## 失败、回退与交付

保留已持久 candidate，错误时停当前范围；不得机械截正文满足字数，不降级为 auto-accept。真实长度/质量判断在 S07/S14，stub 不冒充模型验收。

必交：正文/两批量生产链 receipts、取消唯一 owner 证明、下游冲突保护用例。按通用合同附 base/dependency/final SHA、实际命令/退出码及 failed/blocked/not-run；代码、确定性、模型、安装资格分别报告。

