# S09B — 连续性、定稿与导入关联的身份接入

执行模型：Sol/high。状态：NOT STARTED；依赖满足后可派发。
Blocked by: S09A, S06B。开工填写依赖实际 SHA。

必读：[通用执行合同](../05-SOL-EXECUTION.md)、[约束性契约](../03-CONTRACTS-AND-GATES.md)（C03/C04；ADR0003/0013/0017）、[执行矩阵](../04-EXECUTION-MATRIX.md)。

## 目标与非目标

角色知情/状态/物品等关联不因改名或来源不明接错人；导入与连续性不将推断洗成作者事实。 DSH、真实小说和正式发布不在本片。

## 文件所有权

`src/services/workflows/commands/finalize-chapter.command.ts`、`import-inference-contract.ts`、`electron/repositories/{finalization-repository,summary-repository,import-global-facts-repository}.ts` 的角色引用适配、`src/shared/finalized-continuity.ts` 及专属 tests。只适配身份，不重建定稿/outbox/import ledger。

你不是唯一开发者；不改其他片/共享区，不回滚他人改动，不再开子代理。共享接线只有一个请求区，由主集成者落地。

## 实施步骤

1. 定稿证据/连续性 projection 用稳定 ID及来源 receipt，display snapshot仅展示；stale/legacy 状态不自动变 author。
2. 导入模型提取到新人物或歧义 name-only 走 proposal，保留原字符串/来源定位；明确已确认的角色映射可复用。
3. 处理未确定关系而不改写原小说；正式事实写入 fail-closed，作者待确认不触发全书自动重抽取。
4. 保持 finalization事务/immutable snapshot/outbox 的 commit边界；导入完成与取消状态仍留给S12，不顺手拆重做。

## 验收与测试

定稿角色已改名、名字交换、旧 summary stale、前章候选不能升格、导入两来源同名、AI静态与动态 provenance、重复定稿 outbox一致；原文 hash不变。

先通过 S00 ABI/environment profile；以下为实施时执行，本轮未运行：

```powershell
pnpm run typecheck
pnpm exec vitest run src/services/workflows/commands/__tests__/finalize-chapter.continuity-facts.test.ts src/services/workflows/commands/__tests__/finalize-postprocess-character.integration.test.ts src/services/workflows/commands/__tests__/import-inference-contract.test.ts electron/repositories/__tests__/finalization-repository.test.ts
```

## 失败、回退与交付

未知身份不写事实，不删除正文或原证据；单字段歧义不得导致静默全项目重建。新 ID数据保持 forward-compatible当前schema。

必交：连续性/定稿/导入 name-only退出表、来源正确性与原子落盘回归。按通用合同附 base/dependency/final SHA、实际命令/退出码及 failed/blocked/not-run；代码、确定性、模型、安装资格分别报告。

