# S02 — 规范 API、资源 URI 与生产消费者迁移

执行模型：Sol/high。状态：NOT STARTED；依赖满足后可派发。
Blocked by: S01。开工填写依赖实际 SHA。

必读：[通用执行合同](../05-SOL-EXECUTION.md)、[约束性契约](../03-CONTRACTS-AND-GATES.md)（主计划 §4.1、C03、矩阵 S02）、[执行矩阵](../04-EXECUTION-MATRIX.md)。

## 目标与非目标

规范 `ai-novel://` 与 `window.aiNovelAPI` 成为运行接口，旧持久引用可读迁入而不继续写；保留虚拟 DB 资源领域权限。 不扩展到 DSH、真实小说改写或正式发布。

## 文件所有权

`src/services/vela-protocol.ts`（迁为拟新增 `resource-protocol.ts`）、`src/shared/project-paths.ts`、`src/services/finalization-client.ts`、`src/components/panels/EditorArea.tsx`、`src/components/editor/{DraftEditor,WorldBuildingEditor,ArchFileViewer,VersionHistory}.tsx`、`src/components/panels/sidebar/ProjectTree.tsx` 和 S00 列出的 URI 消费者。限资源/API 适配，不改预算/内容逻辑；preload/IPC/类型声明接线向主集成者申请。

你不是唯一开发者；不改其他片/共享区，不回滚他人改动，不再开子代理。接线以本合同的单一共享请求交主集成者。

## 实施步骤

1. 实现 typed parser/formatter，区分 core/draft/manuscript/revision/review；保留角色只读投影，不映射成任意可写文件。
2. 逐项迁移 UI 打开/历史 tab/版本比较、workflow/Agent/footer smoke 消费点；旧 URI 仅经集中 legacy parser 读，序列化新状态只产生新 URI。
3. bridge expose、类型、IPC client 和 direct finalization/release smoke 一次接入主集成者；新接口缺失要正确报启动错，不偷偷 fallback 到旧写接口。
4. 扫描 CSS/localStorage/DOM 标识先登记；本片改活跃资源消费者，死代码和锁文件集中 S13 处理。

## 验收与测试

core 五类资源打开/保存/只读/比较/项目切换；旧 URI 复开生成规范引用；角色投影写入拒绝；外部路径/越权资源拒绝；UI 当前 dirty text 不被转换覆盖。

命令须先通过 S00 的 ABI/environment profile；以下为实施时执行，本轮未运行：

```powershell
pnpm run typecheck
pnpm exec vitest run src/services/__tests__/vela-protocol-character-roster.test.ts src/services/__tests__/finalization-client-session.test.ts src/services/__tests__/ipc-client-project-session.test.ts
pnpm exec vitest run --config vitest.browser.config.ts src/components/editor/__tests__/VersionHistory.locale.browser.tsx
```

## 失败、回退与交付

保持集中 read-only legacy parser 到最终退出；入口接入失败不切换新写接口。列明测试改名对应，不能运行已删除路径而计通过。

必交：URI/bridge 消费者逐项覆盖表、新读写权限用例、一个共享接线请求/receipt；磁盘格式迁移不属本片。 另按通用合同交 base/dependency/final SHA、测试退出码/入口覆盖及 failed/blocked/not-run。代码检查、确定性、真实模型和桌面资格不能互相代替。

