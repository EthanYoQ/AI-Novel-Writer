# S12 — 导入 effect ledger、取消和进度收敛

执行模型：Sol/high。状态：NOT STARTED；依赖满足后可派发。
Blocked by: S11, S09C。开工填写依赖实际 SHA。

必读：[通用执行合同](../05-SOL-EXECUTION.md)、[约束性契约](../03-CONTRACTS-AND-GATES.md)（C01/C02/C04/C07 M04；保留真实外部边界）、[执行矩阵](../04-EXECUTION-MATRIX.md)。

## 目标与非目标

去掉重复完成/取消真相与脆弱客户端expiresAt比较，保留必要身份、server TTL、原子提交和源稿保护。 DSH、真实小说改写与正式发布不在本片。

## 文件所有权

`electron/repositories/import-run-repository.ts`、M04 function、`electron/controllers/import-controller.ts`；`src/services/workflows/{import-run-orchestrator,import-workflow,import-derived-refresh}.ts`、`commands/import-novel.command.ts`、`src/components/dialogs/ImportNovelDialog.tsx`、workflow-store仅导入分支及tests。

你不是唯一开发者；不改其他片/共享区，不回滚他人改动，不再开子代理。共享接线只交一个明确请求给主集成者。

## 实施步骤

1. 先给completed_batches、prepared/committed receipt、stage/进度字段做等价/真实消费者表；不能以代码量直接决定删除。
2. 主Run owner裁决执行取消/终态；import ledger保存实际effect与幂等提交，UI进度从effect派生。迁移历史状态保留不确定项，禁止按百分比猜完成。
3. 租约保持owner+epoch+服务端有效期；客户端expiresAt只显示不作为第二写权限。缩小锁范围需证明真实冲突域，不盲移除项目保护。
4. 重复/并发提交、取消与commit竞争、重启恢复只产生一次effect；身份proposal仍走S09批准，事实来源不变化。M04在M03后注册。

## 验收与测试

旧表/ledger组合的历史迁移、complete/partial/unknown、重复或迟到IPC、heartbeat延迟、同项目冲突、不同项目隔离、cancel前后commit、角色批准未完成、外部授权过期；原稿不变且完成项不重做。

先通过 S00 ABI/environment profile；以下为实施时执行，本轮未运行：

```powershell
pnpm run typecheck
pnpm exec vitest run electron/repositories/__tests__/import-run-state-machine.test.ts electron/repositories/__tests__/import-run-execution-lease.test.ts electron/repositories/__tests__/import-run-orchestrator-integration.test.ts src/services/workflows/__tests__/import-run-orchestrator.test.ts
pnpm exec vitest run --config vitest.browser.config.ts src/components/dialogs/__tests__/import-novel-resume.browser.tsx
```

## 失败、回退与交付

先影子只读对照后删除重复字段；不能并存两套可写完成状态。未知历史保留诊断/恢复，不用重跑覆盖。M04数据变更后forward-fix，不退旧库。

必交：字段真相/派生/删除表、M04迁移、取消单owner及锁/CAS最小化证据。按通用合同附 base/dependency/final SHA、实际命令/退出码及 failed/blocked/not-run；代码、确定性、模型、安装资格分别报告。

