# S14B — 中文三场景三章对照与写作质量裁决

执行模型：Sol/high。状态：NOT STARTED；依赖满足后可派发。
Blocked by: S14A。开工填写依赖实际 SHA。

必读：[通用执行合同](../05-SOL-EXECUTION.md)、[约束性契约](../03-CONTRACTS-AND-GATES.md)（C06全部规则；C01实验请求会计）、[执行矩阵](../04-EXECUTION-MATRIX.md)。

## 目标与非目标

判断架构调整是否实际改善小说质量，并核实本轮Bug是否复现；严格区分未发现与证明不存在。 DSH、真实小说改写与正式发布不在本片。

## 文件所有权

只读运行已在S14A前合入的 `scripts/quality-modernization-run.mjs` / `protocol.json`；本片只拥有隔离的合成运行数据/private receipts与匿名盲评包。不能修改任何tracked runner/protocol/生产代码；必要适配退S00并重走S14A冻结。

你不是唯一开发者；不改其他片/共享区，不回滚他人改动，不再开子代理。共享接线只交一个明确请求给主集成者。

## 实施步骤

1. 显式读取baseline/candidate execution-targets与parity receipt：baseline为S00冻结旧实现，candidate为S14A的subjectSha；核对实际启动路径/产物/driver，不相信arm标签。核对剩余80次总帽，两格式由同语义源构造且根隔离，3×3章两臂各自维护前章谱系并同时间窗交错。
2. 使用已批准的硅基流动DeepSeek-V4-Flash及冻结非秘密参数；不输出key、不混入真实小说、不擅自换模型。模型漂移/不可用单栏记录。
3. 逐章核对±20%字数/全部事件/身份时间知情物品/计划历史/复述oracle；保留首跑失败、中断与重试，不挑优样本。
4. 两名独立非实现评审者逐章对自然度/节奏/动机盲比；分歧一次仲裁，无条件则inconclusive。按C06机械聚合，作者可决定采用但不改写实验结论。
5. 每个请求receipt绑定arm、实际target code/artifact/driver SHA、candidate subjectSha与fixture parity ID。目标相同、baseline不可运行、parity失败或subject树变化立即blocked/inconclusive；不拿S00历史输出冒充最终baseline。主线程派发盲评者/仲裁者，本worker不得再开子代理，verdict前不揭盲。

## 验收与测试

任一候选章事实错/长度不符/必需事件遗漏即失败；每个文学维度逐章非劣、至少两组各一章一致优才称小样本改善；缺章或调用不足不通过，不宣称全模型/长篇普遍有效。

先通过 S00 ABI/environment profile；以下为实施时执行，本轮未运行：

```powershell
node scripts/quality-modernization-run.mjs --help
node scripts/quality-modernization-run.mjs --protocol test/fixtures/novel-quality-modernization/protocol.json --targets .runtime/.cache/novel-quality-modernization/execution-targets.json --phase full --dry-run
node scripts/quality-modernization-run.mjs --protocol test/fixtures/novel-quality-modernization/protocol.json --targets .runtime/.cache/novel-quality-modernization/execution-targets.json --phase full
```

## 失败、回退与交付

runner/protocol/双目标在冻结前就绪。help/dry-run零模型调用，dry-run必须实际验证两目标与parity；真实run前明确剩余额。任何tracked适配需求停止并退原owner→S14A，新subjectSha须重跑B/C/D或标未完成，旧证据不自动继承；保留失败历史，不能扩大调用帽掩盖不足。

必交：逐章oracle、匿名原文/哈希、两评审+仲裁、请求/usage账、供应商状态；独立输出failed/inconclusive/non-inferior/improved，不仅给总pass。按通用合同附 base/dependency/final SHA、实际命令/退出码及 failed/blocked/not-run；代码、确定性、模型、安装资格分别报告。
