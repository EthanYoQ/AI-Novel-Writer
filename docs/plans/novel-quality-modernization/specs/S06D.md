# S06D — Agent 生成与工具子任务归同一父动作

执行模型：Sol/high。状态：NOT STARTED；依赖满足后可派发。
Blocked by: S06C。开工填写依赖实际 SHA。

必读：[通用执行合同](../05-SOL-EXECUTION.md)、[约束性契约](../03-CONTRACTS-AND-GATES.md)（C01/C02；ADR0008/0015）、[执行矩阵](../04-EXECUTION-MATRIX.md)。

## 目标与非目标

Agent 可编排已有业务，但不能另起 session 逃预算/终态或用写文件工具篡改事实。 DSH、真实小说和正式发布不在本片。

## 文件所有权

`src/stores/agent-store.ts`、`src/services/agent/tools/start-workflow.tool.ts`、Agent 层调用 generation runtime 的 S00 已登记 adapter、专属测试。不改 MCP 权限、Skill 执行能力或 generic file-write 权限。

你不是唯一开发者；不改其他片/共享区，不回滚他人改动，不再开子代理。共享接线只有一个请求区，由主集成者落地。

## 实施步骤

1. 主动作向工具派生工作流传经主进程验证的 root lineage，禁止调用者自报预算/owner。
2. 工具修复、重复调用、取消、renderer 重启均复用持久账；未知发送不以同 attempt 自动重发。
3. 冻结 Skill 只作为阶段 prompt，保持模型租约；工具与隐藏 reasoning 不保存为用户小说成果。
4. 全量核销 S00 生成消费者：凡未接入的实际入口明确阻断 S07，而不是仅测 agent-store 一个路径。

## 验收与测试

Agent→start-workflow→draft/planning 子调用、嵌套工具返回错误、取消/重复/重启、不同项目伪 root、旧 epoch；generic write 不能写 canonical roster/draft，Skill 无执行扩权。

先通过 S00 ABI/environment profile；以下为实施时执行，本轮未运行：

```powershell
pnpm run typecheck
pnpm exec vitest run src/stores/__tests__/agent-generation-runtime.test.ts src/services/agent/tools/__tests__/issue-90-agent-project-actions.repro.test.ts src/services/agent/tools/__tests__/agent-root-action.test.ts
```

## 失败、回退与交付

不允许 fallback 到未计账 provider；单一入口阻塞保留候选和账本。agent-root-action 测试拟新增。

必交：所有生成入口归一完成表、Agent root/能力边界回归；不宣称 Agent 文学创作质量已验证。按通用合同附 base/dependency/final SHA、实际命令/退出码及 failed/blocked/not-run；代码、确定性、模型、安装资格分别报告。

