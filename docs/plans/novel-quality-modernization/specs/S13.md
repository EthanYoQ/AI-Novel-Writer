# S13 — Vela 最终退场与无效代码清除

执行模型：Sol/high。状态：NOT STARTED；依赖满足后可派发。
Blocked by: S12, S07。开工填写依赖实际 SHA。

必读：[通用执行合同](../05-SOL-EXECUTION.md)、[约束性契约](../03-CONTRACTS-AND-GATES.md)（主计划 §1/4.7、C09；S07/S10B/S11三个early gate已通过）、[执行矩阵](../04-EXECUTION-MATRIX.md)。

## 目标与非目标

全量退出旧运行能力/标识并清掉无效残留，保留集中legacy导入与用户数据；不是只做全局改名。 DSH、真实小说改写与正式发布不在本片。

## 文件所有权

S00全量台账明确归本片的剩余运行消费者、`src/components/editor/{MonacoDiffViewer,DiffViewer}.tsx`候选死模块、无消费者CSS片段/常量、拟新增 `scripts/check-legacy-boundaries.mjs`及其tests。package/lock/.github/.release最终接线主集成者独占。

你不是唯一开发者；不改其他片/共享区，不回滚他人改动，不再开子代理。共享接线只交一个明确请求给主集成者。

## 实施步骤

1. 复核三个early gate及所有生产消费者状态；没通过不得进入最终退场。对S00词法/行为/资产三表逐项terminal disposition。
2. 所有正常API/URI/新数据路径/日志前缀/DOM/localStorage状态转规范契约；旧持久值经显式迁移，不长期保留fallback写旧路径。
3. 验证动态import/barrel/glob/构建/UI无消费者再删Monaco/旧Diff及对应依赖、仅存在性测试、死CSS片段。活跃CodeMirror/ThreeWayMerge/合法第三方实现不重写。
4. 添加有理由的legacy允许表：隔离importer、fixture、历史/许可；不用排除整个src/electron掩盖命中。未知Vela继承来源如实说明，不声称clean-room/改名改变许可。
5. 与主集成者完成所有 tracked release/smoke/workflow 适配并在 S14A 冻结前验证。S14D 只读资格执行，不能在冻结后改 workflow 制造不同 subjectSha 的“同版”证据。

## 验收与测试

每条生产消费链已转新契约，legacy parser只读并隔离；新项目不产生旧路径/API，旧导入仍支持；编辑/版本diff/角色图/模型/MCP/Skill/更新smoke不回归；DSH未变化。

先通过 S00 ABI/environment profile；以下为实施时执行，本轮未运行：

```powershell
pnpm run typecheck
pnpm run check:i18n
node scripts/check-legacy-boundaries.mjs
pnpm run build
```

## 失败、回退与交付

新checker拟新增，构建不等于安装版资格；删代码前留git diff及消费者证据，锁文件只由集成者按依赖移除更新。不能删LICENSE、作者文本、未知数据或真实备份。

必交：Vela词法退出表+旧能力替换表+asset核销表、具体删除清单及可恢复方式、构建证据；现状质量不达标则blocked。按通用合同附 base/dependency/final SHA、实际命令/退出码及 failed/blocked/not-run；代码、确定性、模型、安装资格分别报告。
