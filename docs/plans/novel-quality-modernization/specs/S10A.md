# S10A — 有来源上下文的纯选择契约

执行模型：Sol/high。状态：NOT STARTED；依赖满足后可派发。
Blocked by: S07, S09C。开工填写依赖实际 SHA。

必读：[通用执行合同](../05-SOL-EXECUTION.md)、[约束性契约](../03-CONTRACTS-AND-GATES.md)（C03/C04；ADR0013/0014/0015/0017）、[执行矩阵](../04-EXECUTION-MATRIX.md)。

## 目标与非目标

先证明什么资料可进入本章，再接模型；选择器不成为第二套小说真相库。 DSH、真实小说改写与正式发布不在本片。

## 文件所有权

拟新增 `src/services/workflows/source-selection.ts`、`context-snapshot.ts` 及专属纯 fixtures；消费 S01 shared SourceRef，不编辑生成入口、repositories、IPC 或已有 prompt 文案。

你不是唯一开发者；不改其他片/共享区，不回滚他人改动，不再开子代理。共享接线只交一个明确请求给主集成者。

## 实施步骤

1. 输入为冻结作者指导/本章事件、身份与事实源引用、模型容量策略；输出 ContextSnapshot include/omit/required coverage/token estimate，不复制另一个可写canonical内容库。
2. 实现候选白名单及其 lineage/hash/状态检查；合法待审前驱可用，废弃/恢复/冲突/跨batch不能按相关性混入。
3. 区分作者/正式历史/未来计划/派生定位/参考，stale摘要回读原证据；角色未知来源不洗白，剧情树不作为正文材料。
4. 先必需后可选选择；必需材料装不下返回可解释 split/capacity conflict，不默默裁掉。位置单位与estimator版本固定。

## 验收与测试

同章两稿、源替换下游冲突、合法batch直接前驱、未来秘密提前、作者规则与Skill冲突、stale摘要、长设定超容量、重复材料；相同输入产生可重放snapshot且不发模型。

先通过 S00 ABI/environment profile；以下为实施时执行，本轮未运行：

```powershell
pnpm run typecheck
pnpm exec vitest run src/services/workflows/__tests__/source-selection.test.ts src/services/workflows/__tests__/context-snapshot.test.ts
```

## 失败、回退与交付

本片纯函数/投影，不写原稿。两份测试拟新增；不为了通过fixture引入全书付费抽取或新检索数据库。

必交：纯contract、可重放source/include/omit矩阵、确定性拒绝/覆盖fixtures。按通用合同附 base/dependency/final SHA、实际命令/退出码及 failed/blocked/not-run；代码、确定性、模型、安装资格分别报告。

