# S01 — 共享领域契约、schema lane 与接入骨架

执行模型：Sol/high。状态：NOT STARTED；依赖满足后可派发。
Blocked by: S00。开工填写依赖实际 SHA。

必读：[通用执行合同](../05-SOL-EXECUTION.md)、[约束性契约](../03-CONTRACTS-AND-GATES.md)（C01–C09、执行矩阵共享 owner）、[执行矩阵](../04-EXECUTION-MATRIX.md)。

## 目标与非目标

冻结最小可执行的类型、唯一 owner、版本顺序、兼容边界与测试接缝；不提前重写全部工作流。 不扩展到 DSH、真实小说改写或正式发布。

## 文件所有权

本片独占通用合同所列拟新增 `src/shared/` 契约、`electron/migrations/registry.ts`/`runner.ts`、`docs/adr/` 新 ADR；所有共享接线由主集成者实施，不向其他 Agent 下放中心文件权限。

你不是唯一开发者；不改其他片/共享区，不回滚他人改动，不再开子代理。接线以本合同的单一共享请求交主集成者。

## 实施步骤

1. 为 Run/rootAction/Attempt/reservation/Artifact、SourceRef/ContextSnapshot、身份 resolver/proposal、ReviewCycle 写类型与拒绝路径。注明 UTF-16 span、原 UTF-8 hash、reasoning 用量口径与 provider adapter 契约。
2. 给 M00→M04 分配连续整数、from/to/事务重启规范；probe→migrate→verify→fence 接口分离。未知高版本拒写；不靠 ad-hoc ALTER 猜补。
3. 固定项目 journal 不随旧目录移动失联的位置、双全局 locator/隔离变量和 generation 读写接口；明确 Windows/macOS flush/rename 的能力探针要求，不虚构平台保证。
4. 新增/supersede ADR，保留既有事实/outbox/Skill/剧情树安全边界。仅添加无行为变化的兼容 seam，运行核心、身份与迁移行为留给各片。

## 验收与测试

contract 类型可编译；重复 reservation、非法候选/身份状态、未知 schema 可用纯 fixture 判拒；schema registry 无重复/跳跃 owner，旧入口回归无行为改变。

命令须先通过 S00 的 ABI/environment profile；以下为实施时执行，本轮未运行：

```powershell
pnpm run typecheck
pnpm exec vitest run src/shared/__tests__/novel-contracts.test.ts electron/migrations/__tests__/runner.test.ts
```

## 失败、回退与交付

新增契约尚未产生用户新数据可移除未消费 seam；新持久数据以后不得退版本。本片测试文件均拟新增。

必交：contracts/module registry、具体 migration 编号表、接线登记、ADR、纯 contract/runner fixtures；本片通过不表示新核心已实现。 另按通用合同交 base/dependency/final SHA、测试退出码/入口覆盖及 failed/blocked/not-run。代码检查、确定性、真实模型和桌面资格不能互相代替。

