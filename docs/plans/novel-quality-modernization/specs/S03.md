# S03 — 全局配置一次性迁移与启动前门禁

执行模型：Sol/high。状态：NOT STARTED；依赖满足后可派发。
Blocked by: S02。开工填写依赖实际 SHA。

必读：[通用执行合同](../05-SOL-EXECUTION.md)、[约束性契约](../03-CONTRACTS-AND-GATES.md)（C08 全局、C09、C07 共享区）、[执行矩阵](../04-EXECUTION-MATRIX.md)。

## 目标与非目标

新全局根无丢配置/密钥地接管且旧根不回灌；任何消费者写入前完成迁移裁决。 不扩展到 DSH、真实小说改写或正式发布。

## 文件所有权

`electron/utils/config-utils.ts`、拟新增 `electron/services/global-data-migration.ts`/`app-data-locator.ts`、`electron/services/{skin-service,update-preferences-store}.ts`、`electron/mcp/mcp-manager.ts`、app-data/llm controller 中 locator 消费点及专属测试。main/IPC 接线只由集成者写。

你不是唯一开发者；不改其他片/共享区，不回滚他人改动，不再开子代理。接线以本合同的单一共享请求交主集成者。

## 实施步骤

1. 从 S00 全局 disposition 逐项处理 config/models/recent-projects、用户 prompts/Skills、MCP、skin、update、产品自有 logs/metadata；共享旧 `.vela` 非本品数据不搬删。
2. coordinator 先于所有默认写入/可写 controller/普通窗口；不在模块 import 时 ensure 新/旧根。未完成只暴露迁移 UI/只读诊断。
3. 实现 legacy source / canonical target 双 locator、generation staging/commit。models 与 config 同 generation 校验默认 ID；同 ID 凭据差异仅显示“凭据不同”，不输出密钥或密钥 hash。
4. 损坏/半目标/未知项保存并裁决；cutover receipt 后旧根变化只提示显式导入。真实 home/appData 和 userData 全部在测试隔离。

## 验收与测试

仅旧、仅新、同 ID 不同凭据、损坏 JSON、每个对象写入点崩溃、默认引用不存在、空目录/半 generation、旧源后来变化；真实用户根 hash/写入监测不变；单实例协调不因改 locator 失效。

命令须先通过 S00 的 ABI/environment profile；以下为实施时执行，本轮未运行：

```powershell
pnpm run typecheck
pnpm exec vitest run electron/utils/__tests__/config-utils.test.ts electron/services/__tests__/global-data-migration.test.ts electron/__tests__/main-skin-startup.test.ts
```

## 失败、回退与交付

切换前可放弃本次 staging，不能覆盖源。切换后不自动回灌旧数据，保留旧根，只在显式对象导入流程解决差异；未获验证不开作者迁移。

必交：全局对象 disposition/冲突矩阵、generation/journal receipts、真实根零写证明、startup shared-wiring；global-data-migration 测试拟新增。 另按通用合同交 base/dependency/final SHA、测试退出码/入口覆盖及 failed/blocked/not-run。代码检查、确定性、真实模型和桌面资格不能互相代替。

