# F05 — Writer完整功能并集、Classic兼容与默认资格

依赖F04。独占覆盖清单、browser/Electron fixture与证据；不在验收片塞入新业务实现。内部顺序固定Preflight→默认激活→Final/post-UI三门，dag.json记录machine-readable gates，所有子门完成才算F05完成。发现问题退原owner。目标是Writer完整主入口承载09/feature-union的全部动作，Classic继续兼容共享业务；不是两边各有一半功能。

1. 消费S00冻结、F04逐动作填实的16组feature-union。每个requiredInWriter动作只能从Writer入口取得receipt，Classic/内部测试API不能代填。共同业务双壳比较结果；Writer专属标签/沉浸/书架/图形呈现只要求Writer完整，不复制Classic布局。U16已是本轮真备份增强，不能用移除disabled占位过门；每个null/not-run缺口均NO-GO。每个action独立owner/uiOwner/qualificationOwner、entry/command/IPC/outcome/fixture/SHA/receipt+stepId，组状态只能由全部必需动作派生；拒绝不相关动作共用同一步证据。S00现有runner/简单校验覆盖负例：仅填组内一项再手写组PASS、U16本地归档错派B02、Classic证据、缺必需层级均拒绝。
2. 相同合成中文fixture在Classic/Writer走实际按钮到统一IPC：新建/打开、配置和模型列表/支持参数、指定范围大纲/续批、world-building恢复/冲突拒写、正文保存/重开/导出、批量两模式、角色导入/图谱/头像、Skill绑定、审稿选择/修稿/no-op/合并/定稿与错误重试。
3. 使用确定性provider/故障fixture比较实际命令、冻结参数、持久成果和候选状态；明确这些不是真模型。两壳不应多发请求或改prompt/预算root。UI诊断复制脱敏、模型名/实际预算参数/finish原因可见，无密钥/小说正文。
4. 高风险共同能力两壳都测：dirty切项目/切壳、保存失败/取消/任务活跃、窗口关闭、未知偏好fallback、#224双入口、100%/系统高DPI、中文IME/撤销/选区/长文输入、图谱“重置”与“删除”区分、重开迁移后的头像。新增功能验收：头像压缩/替换/移除/图谱批量读与归档恢复；统一标签/沉浸不丢状态；书架授权只读/零写/缓存不含正文路径；六工序不假完成；每类编辑器保存失败保稿；C16自动derived正例与author冲突反例；U16两个隔离profile的实际打包入口+受控WebDAV备份/恢复新副本、凭据/冲突/中断/损坏/跨项目原稿保护（mock网络不能代此服务闭环）。
5. 颜色/背景采用关键页面pairwise可读性与portal检查，不做全部颜色×全部功能×全部真模型乘法。捕获实际安装环境清晰截图后才给视觉结论；合成mock界面不能代替。
6. 前五步形成Preflight准入receipt后，主集成者激活releaseDefaultShell=writer；shellPreference仍unset/作者显式值分开，不把默认写成作者选择。随后在激活及全部F04/F05 tracked适配完成的集成SHA验证新装/unset→Writer、明确v1/v2→对应选择、颜色字体不变、Classic可回切及同canonical根，形成Final。Preflight失败不激活；Final失败NO-GO不发布，修复后重取SHA。S14A只核验默认已激活，不在三门后再晚改。
7. **强制post-UI重新资格**：主线程召回原S07/S10B/S11 owners，分别在同一第6步集成SHA重建candidate manifest，消费S00仍可执行的真实baseline；先实际双目标零模型dry-run/parity，再执行early-budget、early-context、early-review各自预注册固定中文两臂案例（milestone=post-ui）。每份receipt绑定实际code/artifact/driver SHA与fixture/arm，postUiIntegrationSha必须等于F05.Final接受SHA。worker不能开子代理，由主线程分派原owners。mock、旧early PASS、历史单臂输出均不替代。
8. S13.requiredGates强依赖三份post-UI receipts；任何相关入口/参数在此后改变都重新资格，不自动继承。S00从原80次总帽预留此必需重跑，包含失败/复核，不加帽、不加最终18章样本量；不足blocked/inconclusive。不额外跑与本片无关的真模型场景，最终S14B仍独立完整双臂。

1000角色图使用无模型合成fixture检查批量/有界IPC计数、窗口渲染/搜索全部人物可达与无事实写入；头像不能为性能删除。高规模历史耗时只是调查线索，实际计时按冻结协议并清楚报告，不宣称本轮已复现。

拟新增src/components/__tests__/appearance-feature-parity.browser.tsx与test/desktop/appearance-parity.fixture.ts（路径由S00确认runner后登记）；执行现有browser/native desktop profile、typecheck/check:i18n，不用passWithNoTests。

性能协议以feature-union.json的editor-absolute-v1为唯一机读值，在S00、未看候选结果前冻结：同机、同一production build（非dev/HMR）、同分辨率/缩放/字体，无模型任务；3000与200000中文单位固定合成文档，Classic/Writer交错，3次warm-up后各7次同一编辑/切页动作，事件起点到对应DOM完成的下一绘制帧为计时点，按units→shell→action(input/selection)保存warmupSamplesMs[3]、rawSamplesMs[7]与longTasksMs[]；checker逐动作校验数量/有限值，自算median/worst/MAD及MAD/median，不接受调用者自报summary或unstable。相对20%也逐input/selection比较；缺selection、少于7样本、高MAD却自报稳定均拒绝。最多原协议一次同条件重跑(rerunIndex≤1)，不能通过丢弃慢样本变稳定。绝对可用性同时成立：两个长度fixture中，两壳的输入/选择响应median≤50ms且7个有效样本的最大值≤100ms；主线程单个长任务不超过50ms；产品目标是普通输入0.05秒级可见反馈、任何有效样本不超0.1秒，阈值不由实现者事后挑选。Writer专属实时预览保持启用。系统中文IME的固定输入/候选选择/撤销回做序列，最终正文/选区与预期精确一致，零丢字、零重复、无半个composition提交。相对门也保留：稳定样本中Writer中位延迟不能恶化超过20%；MAD/median>10%或后台负载不一致的测量仅允许一次同协议重跑，仍不稳定则inconclusive，不把偶然抖动判成确定Bug。负例“两壳同慢”“IME丢字但耗时合格”“关闭实时预览才变快”一律拒绝；用现有runner，不新建benchmark平台。

交付：逐格证据、实际desktop窗口/入口、未测平台、默认NO-GO/可进入最终资格。只要Writer必需功能/子动作缺接线、被隐藏/禁用、dirty丢失或review阻断，NO-GO；不能以Classic回退替代。不是F05通过就立即发布。

## 执行合同

模型：Sol/high。你不是唯一开发者，不回滚他人修改，禁止再开子代理。只改本片独占文件；共享区仅交一个接线请求给主集成者。先读本包01/04/05、旧通用执行合同及依赖Spec。源路径以开工实际工作树为准；拟新增文件必须登记。状态NOT STARTED，下面是要执行的检查，不是已通过证据。完成给出依赖/最终SHA、实际命令/退出码、证据/未执行项；密钥/作者原稿/本地路径不上传。DSH排除、产品场景仅中文。
