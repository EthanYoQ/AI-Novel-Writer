# F03 — 必须保留的完整头像功能与资产安全

依赖S12、F01（间接依赖S08/S09ID与S04物理迁移）。独占拟electron/services/character-asset-service.ts、资产repository/migration M05、donor avatar controller/hook的受控适配及专属测试；registry/DB/IPC注册由集成者。不引入第二套角色事实或通用资产平台。

**硬交付U10**：不能删除/隐藏/禁用头像或仅迁移文件不接UI。Writer与Classic在同一人物档案能选择、预览、保存、替换、移除并在图谱显示；F04接线，F05从实际入口验收。

1. 消费S00/S04旧资产manifest与S08稳定ID，设计canonical .ai-novel内受控头像路径；不以可变姓名作为key。
2. 保留choose预览不提交、明确保存才生效；复用文件grant、魔数/现有4MB输入边界/路径检查。保留现有nativeImage最长边256px压缩：PNG仍PNG、其他合法输入转JPEG82；压缩失败保持原始合法图或明确失败，不损坏旧引用。作者取消不得产生正式引用；修复可改变内部编码实现，不能移除压缩/预览行为。
3. 采用现有原子文件写/DB事务接缝：临时写校验→安装完整文件→事务更新引用；DB失败撤销本attempt可证明新建文件，崩溃未清理项只记录可回收孤儿，不误删旧/共享引用。不要为头像另建全项目分布式事务框架。
4. 迁移旧avatar引用/文件原字节，改名/重名不串图；孤儿、姓名hash碰撞、多义记录保留并显式待确认，不猜测绑定。头像删除/角色删除有明确回收规则，不能跟随symlink读取项目外。
5. M05函数/fixture由本片给出，唯一schema lane在M04后注册；支持from/to与probe fingerprint。运行期commit与M05迁移是两个显式入口：M05仅收S04的sourceSnapshot/stagingTargetRoot capability，不读runtime/default canonical locator。文件、DB引用、孤儿清单全在staging，不改来源；target-installed前不得创建live .ai-novel资产。文件/引用/hash进S04 journal，verified后关闭源/staging DB和文件句柄再cutover。S08保留旧资产关联→新ID映射，M05不得猜名字。最终S04转换到M05仍同registry；unknown fork保全/blocked。
6. 项目移动、受控项目副本/导入与projectId/characterId改变时头像引用稳定。现有merged-md/split-md/txt只有正文，不加头像附件，不改正文export-service/ExportDialog；B01独占完整项目归档，F03提供portable资产表/引用映射与合成fixture，必须包含头像，不把它当可重建cache。头像不进角色事实/prompt/hash投影；首页旧私密缓存按C13处置。
7. 人物卡和图谱共用按project/session+characterId+assetRevision的读取接缝；批量或有界窗口加载，不为每个人物发一条IPC。缓存仅内存且项目切换/替换/删除失效；F04大图可只绘制相关窗口，但全部人物仍可查找/进入。不是删除头像来省成本。
8. 向S04 owner/集成者提交唯一runner适配请求，并以F03.m05-integrated作为本片完成门：同一registry最终target已包含M05，S04资产/portable表包含头像字节/引用/孤儿/歧义，tracked project-migration-acceptance已适配且可构造/验证M05，交实际集成SHA/manifest/validator receipt。B01开工必须消费这个门，不能等S14A才接。此门覆盖最终M05的known donor、unknown fork、姓名碰撞和每个文件/DB/journal注入点，证明legacy-isolated前live canonical零创建/零写、只留任务staging且原字节不变。不能到S14C才补tracked脚本；S14C只在最终subjectSha重跑真实升级/恢复资格。

拟新增：electron/services/__tests__/character-asset-service.test.ts、electron/migrations/__tests__/character-assets-migration.test.ts；适配donor avatar controller/hook已有测试。用合成图片测试成功/取消/DB失败/崩溃孤儿/rename/重名/删除/受控副本/unknown旧库/越界和staging写根，执行相关vitest+typecheck；Electron native profile单独验证真实grant/文件入口。

回退：新资产写入后不回旧schema；保留失败候选/旧引用，允许forward fix。没有迁移原件/图片字节证据、头像压缩/替换/移除/图谱读、B01归档恢复契约不得通过。任何U10动作缺失仍是未完成，Classic可用不代替Writer。

## 执行合同

模型：Sol/high。你不是唯一开发者，不回滚他人修改，禁止再开子代理。只改本片独占文件；共享区仅交一个接线请求给主集成者。先读本包01/04/05、旧通用执行合同及依赖Spec。源路径以开工实际工作树为准；拟新增文件必须登记。状态NOT STARTED，下面是要执行的检查，不是已通过证据。完成给出依赖/最终SHA、实际命令/退出码、证据/未执行项；密钥/作者原稿/本地路径不上传。DSH排除、产品场景仅中文。
