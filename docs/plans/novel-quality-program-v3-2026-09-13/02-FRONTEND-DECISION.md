# 前端接入：完整功能并集，不是仅移植外观

## 1. 技术与证据

贡献目录是React 19 + TypeScript + Vite + Zustand + Electron，index.html只是React入口。可以移植现成组件/CSS，不需要换语言；不应直接套HTML或复制整个工程。

取证业务树HEAD：731bda13ff9197d0abefaa353139df1359ce75cb；当时origin/master 2264390d6fb8b052cc14736d544df0cc74516649同树。donor基于v1.1.0，晚近恢复/上下文修复不能倒退。旧三方比较在src/electron/scripts/test/public/.storybook范围为726相同、67不同、67 donor-only、2 current-only；是差异范围，不是全仓逐行通过。S00开工重取实际SHA/manifest。

本轮新增[功能来源调查](evidence/donor-feature-union.md)仅是源码/历史说明核对，未运行Electron、模型或真实项目。上一轮renderer-only只看过首页/设置/外观切换，截图模糊不能作视觉精度PASS。不得把这些变成移植后功能已通过。

## 2. 明确保留

Writer必须自身提供下列能力的实际入口，而不是引导回Classic：

- 原有项目新建/打开/保存/导入导出/最近记录、配置与模型/Prompt/Skill/知识、架构/大纲范围与续批、蓝图/正文/两种批量、角色/图谱、审稿/修稿/定稿、历史/版本/诊断和恢复。
- 新的Writer布局、书脊导航/统一标签、沉浸写作、真实书架统计/跨项目速览、六阶段进度、实时Markdown预览/纸页呈现、中文IME/选区/缩进/字体/保存反馈。
- **完整角色头像功能**及新关系图：头像选择预览、提交、压缩、显示/替换/移除、改名重名/重开/迁移；关系图五档、换中心、拖动/平移/缩放/复位、侧栏/打开档案，原关系编辑功能仍可用。
- 本轮增强：从文本提取可编辑角色卡、随定稿自动补充派生画像、项目完整备份/恢复、手动WebDAV云存档。

逐动作门在[09功能并集](09-FEATURE-UNION.md)及机器可读feature-union.json。已有功能不能删减；可以替换危险实现、修展示、改按钮位置，但必须留下同等可用能力。允许删除的只是已证明无消费者代码、disabled空占位和假demo数据，而不是用户可用功能。

## 3. donor缺陷与必须修复的落点

| 取证发现 | 最小修复方向 | owner |
| --- | --- | --- |
| 切项目先clearProjectTabs，删除dirty tabs/save handlers/ledger，缺保存门 | 所有项目切换统一保存/放弃/取消；成功后才清；失败/旧异步回包不丢稿串项目 | F04 |
| donor缺最新世界观候选重校验与按章synopsis | 三方合并，消费最新S06/S10接缝，不带回旧command | S06/S10、F04 |
| 头像旧Vela路径、姓名hash、先文件后DB、全员N次IPC | 稳定ID、M05 staging迁移、可恢复提交/有界读取；保留压缩和所有操作 | F03 |
| 项目速览接受renderer任意path；readonly失败后普通可写打开；旧.vela定位 | 使用授权recent-project capability与canonical只读聚合；失败unavailable，不迁移/写入未打开项目 | F04、中央IPC owner |
| overview缓存含正文尾句/路径 | 保留实时速览，退出私密持久缓存；摘句仅当前授权项目内存读取，失效即清 | F04 |
| FlowStages以“任一章已做”表示整阶段完成 | 显示未开始/进行中/完成及已做/计划数量；总目标未知显示未知，不造100% | F04 |
| DraftEditor等保存失败未明确反馈 | dirty/saving/saved/failed真实状态，失败保稿可重试；快捷键/按钮/退出统一 | F04 |
| 预览、字体/IME/撤销、长文有历史性能疑点 | 保留新编辑体验，实际中文长短文定位/修复；不能关闭预览来过门 | F04/F05 |
| 图谱重置语义与删除角色混淆；大图全员请求 | 重置布局零事实写；删除明确确认；分页/有界渲染仍能找到全部角色并显示头像 | F03/F04 |
| #224设置双入口白屏，关联PR未过Windows CI | 同一设置实例/有效默认tab/关闭重开状态，实际Writer/Classic两入口验证 | F04/F05 |

以上以源码反例/历史记录为线索，未标本轮运行复现。F04每项先做最小确定性生产入口复现；若最新代码已修，复验后复用，不重写。修完相关回归并独立review才交F05。

## 4. 不将占位误作功能，但也不偷删需求

旧“备份”按钮确实disabled/no-op；它不是可用功能证据。**v3因#213明确纳入真实备份功能，B01/B02/F04要将对应入口接通，而不是继续隐藏占位后声称云存档完成。** 未完成只可开发期显示说明，发布门仍NO-GO。

“第2卷·疑云”“设定0冲突”“续写38%”等demo、未实现的世界词条卡、未落地的审美愿景不自动变成必做功能；显示真实统计/现有世界观和知识能力。大图按需绘制是修性能的可选实现，不承诺一个新图数据库或画布平台。

头像不随TXT/Markdown正文导出，不意味着删除头像；B01项目归档和B02云存档必须保全头像字节/关联。未知素材许可阻塞该素材发布，优先同功能合规替代，不能借此删掉整个交互功能。

## 5. 默认策略是交付要求，不是立即上线

完整Preflight通过→激活Writer默认→同SHA Final和post-UI三门→清理/审查/冻结→中文质量/旧版升级/三个安装目标→授权发布。功能缺失即NO-GO，不接受“只发布经典也算完成”。

Classic可随时切回同一新内核；不会回旧Vela路径/数据库。保留作者明确Classic/Writer选择和全部有效外观偏好。Writer专属布局只在Writer测试其完整体验，共享业务两壳必须结果等价；不要求Classic复制Writer每一种新布局。
