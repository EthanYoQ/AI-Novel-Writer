# F05 — 两外壳能力等价与默认准入

依赖F04。独占覆盖清单、browser/Electron fixture与证据；不在验收片塞入新业务实现。内部顺序固定Preflight→默认激活→Final/post-UI三门，dag.json记录machine-readable gates，所有子门完成才算F05完成。发现问题退原owner。目标是证明同一内核有两套完整可操作的外壳。

1. 将02的15能力展开为两壳必需动作表，每项标源码/组件/真实Electron/安装版/模型证据等级。备份占位标“非已实现能力/已移除”，不能算通过。
2. 相同合成中文fixture在Classic/Writer走实际按钮到统一IPC：新建/打开、配置和模型列表/支持参数、指定范围大纲/续批、world-building恢复/冲突拒写、正文保存/重开/导出、批量两模式、角色导入/图谱/头像、Skill绑定、审稿选择/修稿/no-op/合并/定稿与错误重试。
3. 使用确定性provider/故障fixture比较实际命令、冻结参数、持久成果和候选状态；明确这些不是真模型。两壳不应多发请求或改prompt/预算root。UI诊断复制脱敏、模型名/实际预算参数/finish原因可见，无密钥/小说正文。
4. 高风险两壳都测：dirty切项目/切壳、保存失败/取消/任务活跃、窗口关闭、未知偏好fallback、#224双入口、100%/系统高DPI、中文IME/撤销/选区/长文输入、图谱“重置”与“删除”区分、重开迁移后的头像。
5. 颜色/背景采用关键页面pairwise可读性与portal检查，不做全部颜色×全部功能×全部真模型乘法。捕获实际安装环境清晰截图后才给视觉结论；合成mock界面不能代替。
6. 前五步形成Preflight准入receipt后，主集成者激活releaseDefaultShell=writer；shellPreference仍unset/作者显式值分开，不把默认写成作者选择。随后在激活及全部F04/F05 tracked适配完成的集成SHA验证新装/unset→Writer、明确v1/v2→对应选择、颜色字体不变、Classic可回切及同canonical根，形成Final。Preflight失败不激活；Final失败NO-GO不发布，修复后重取SHA。S14A只核验默认已激活，不在三门后再晚改。
7. **强制post-UI重新资格**：主线程召回原S07/S10B/S11 owners，分别在同一第6步集成SHA重建candidate manifest，消费S00仍可执行的真实baseline；先实际双目标零模型dry-run/parity，再执行early-budget、early-context、early-review各自预注册固定中文两臂案例（milestone=post-ui）。每份receipt绑定实际code/artifact/driver SHA与fixture/arm，postUiIntegrationSha必须等于F05.Final接受SHA。worker不能开子代理，由主线程分派原owners。mock、旧early PASS、历史单臂输出均不替代。
8. S13.requiredGates强依赖三份post-UI receipts；任何相关入口/参数在此后改变都重新资格，不自动继承。S00从原80次总帽预留此必需重跑，包含失败/复核，不加帽、不加最终18章样本量；不足blocked/inconclusive。不额外跑与本片无关的真模型场景，最终S14B仍独立完整双臂。

拟新增src/components/__tests__/appearance-feature-parity.browser.tsx与test/desktop/appearance-parity.fixture.ts（路径由S00确认runner后登记）；执行现有browser/native desktop profile、typecheck/check:i18n，不用passWithNoTests。

性能协议在S00预注册：同机、同一production build（非dev/HMR）、同分辨率/缩放/字体，无模型任务；3000与200000中文单位固定合成文档，Classic/Writer交错，3次warm-up后各7次同一编辑/切页动作，事件起点到对应DOM完成的下一绘制帧为计时点，保存原样本/median/MAD。稳定样本中Writer中位延迟不能恶化超过20%；MAD/median>10%或后台负载不一致的测量仅允许一次同协议重跑，仍不稳定则inconclusive，不把偶然抖动判成确定Bug。用现有runner，不新建benchmark平台。

交付：逐格证据、实际desktop窗口/入口、未测平台、默认NO-GO/可进入最终资格。只要必须功能缺接线、dirty丢失或review阻断，NO-GO；不是F05通过就立即发布。

## 执行合同

模型：Sol/high。你不是唯一开发者，不回滚他人修改，禁止再开子代理。只改本片独占文件；共享区仅交一个接线请求给主集成者。先读本包01/04/05、旧通用执行合同及依赖Spec。源路径以开工实际工作树为准；拟新增文件必须登记。状态NOT STARTED，下面是要执行的检查，不是已通过证据。完成给出依赖/最终SHA、实际命令/退出码、证据/未执行项；密钥/作者原稿/本地路径不上传。DSH排除、产品场景仅中文。
