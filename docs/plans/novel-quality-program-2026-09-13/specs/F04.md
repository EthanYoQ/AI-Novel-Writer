# F04 — 业务页面接入与未保存安全门

依赖F02、F03、S12。这是UI接线片，不重写生成业务。接力拥有EditorArea、editor-store展示状态、project切换UI接缝、共享业务页面、CodeMirror适配、关系视图、SettingsModal及专属测试。生成/ID/上下文/审稿持久语义仍由原owner；中央IPC/App接线由集成者。

按四个可验证检查点逐个提交，不允许一口气目录覆盖：
1. **项目转换门**：复用/扩展既有exit-save registry成共享的transition检查入口，不新建重复状态库。新建/打开/最近/书架/关闭项目先检查全部dirty tabs、后台ledger、未保存配置/角色与未发送助手输入；保存并切换/放弃并切换/取消。任一保存失败或切换期间源内容/lease变化则原项目原内容不动。只有明确结果后才清旧session视图，旧异步结果不污染新项目。删除donor先clear再补救行为。
2. **编辑/布局**：两壳复用同一编辑和save owner，优先让BusinessSurface/EditorArea保持稳定React owner/key，只换chrome/layout，从而自然保留buffer/selection/undo/助手输入；不造undo序列化仓库、不让所有页面永久挂载。确实不能保挂的极少局部状态才明确保存。中文IME组合期间推迟切换到compositionend或显式取消，不能截断输入。字体遵循F01，不吞作者选择。
3. **业务页面**：按02能力表移植外观，保留最新world_building_partial_result、恢复授权/冲突候选与synopsisForDraftChapter；适配S06运行、S09ID提议、S11finding，批量失败/取消/定稿状态和恢复操作不丢。角色导入保留#212全部错误/输入/隔离行为。
4. **图谱/设置/资料**：图谱接稳定ID，布局不回写事实；“重置布局”不删人，“删除全部角色与关系”明确二次确认并走正式能力。接F03头像；overview无私密持久尾句；#224两设置入口/默认tab/关闭重开/参数控件接共享状态；支持参数明列，不新增任意provider字段透传。Skill只接现有安装/绑定/冻结，不执行文件脚本。

拟新增tests：
- src/components/panels/__tests__/project-transition-unsaved.browser.tsx
- src/components/editor/__tests__/writer-editor-state.browser.tsx
- src/components/settings/__tests__/settings-two-entry.browser.tsx
并复用当前world-building-recovery.browser.tsx、world-building-recovery.command.test.ts及相关角色/审修/Skill现有测试。每检查点跑相关vitest/browser和typecheck；最终全覆盖F05接收。

至少覆盖正文/角色/配置/背景ledger dirty、保存失败/取消、并发旧回包、切壳不变根预算/任务。真实Windows中文IME/撤销/拖选属于F05安装入口核验，模拟composition只算组件证据。

回退：未完成接线不启用Writer默认；恢复至Classic同内核，而非丢稿后加载旧DB。发现核心语义缺陷回原Spec修补并附依赖SHA，不自行再造工作流。

## 执行合同

模型：Sol/high。你不是唯一开发者，不回滚他人修改，禁止再开子代理。只改本片独占文件；共享区仅交一个接线请求给主集成者。先读本包01/04/05、旧通用执行合同及依赖Spec。源路径以开工实际工作树为准；拟新增文件必须登记。状态NOT STARTED，下面是要执行的检查，不是已通过证据。完成给出依赖/最终SHA、实际命令/退出码、证据/未执行项；密钥/作者原稿/本地路径不上传。DSH排除、产品场景仅中文。
