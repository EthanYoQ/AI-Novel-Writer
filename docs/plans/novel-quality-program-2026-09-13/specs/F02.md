# F02 — 选择性移植React外壳和样式

依赖S02、F01。独占components/layout/v2、pages/v2展示组件、styles/redesign、已批准合法素材/专属测试。不改共用业务页面/command/store/DB；App/main/index接线交集成者。

1. 消费S00三方manifest，每个donor UI文件记录采用/适配/拒绝及hash；不复制整个src或electron。
2. ShellV2作为写手外壳，接同一Sidebar/Editor/AI/Bottom ReactNode或明确slots。移植title bar/rail/tabs容器；renderer-only可展示，不伪造生成/保存数据。
3. 色彩令牌和portal样式依F01；保留Classic能力，不用全局CSS无条件覆盖旧壳。图标lucide；字体/图片许可与归属未确认不能带入发布包。
4. 首页统计只读props，尾句不写localStorage；未加载/无项目/未知数据明确空态，不显示假进度。
5. 移除disabled no-op备份占位，不新增备份系统；导出/新建/打开/设置这些已有能力必须保留可接的action。
6. 不偷带旧velaAPI/URI/localStorage路径，不导入donor command/store替换当前版本。已规范化API只走S02 facade。

拟新增测试：src/components/layout/v2/__tests__/writer-shell.browser.tsx。两壳空态/导航slot、四颜色portal代表场景、键盘焦点、窄于支持最小窗口提示或布局；样式资源相对打包路径。执行browser用例、typecheck/check:i18n；该fixture只证明外壳结构，不当端到端功能。

回退：Classic同内核仍可用，不回旧App/数据格式。交付完整donor采用清单与共享接线请求、清晰截图；模型调用为0。

## 执行合同

模型：Sol/high。你不是唯一开发者，不回滚他人修改，禁止再开子代理。只改本片独占文件；共享区仅交一个接线请求给主集成者。先读本包01/04/05、旧通用执行合同及依赖Spec。源路径以开工实际工作树为准；拟新增文件必须登记。状态NOT STARTED，下面是要执行的检查，不是已通过证据。完成给出依赖/最终SHA、实际命令/退出码、证据/未执行项；密钥/作者原稿/本地路径不上传。DSH排除、产品场景仅中文。

